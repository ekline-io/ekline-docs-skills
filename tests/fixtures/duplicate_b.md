# Setup instructions

To install the package you need to download it from the official website. Make sure you have the latest version of Node.js installed on your system. Run the installation command and wait for it to complete. After installation you should verify that everything works correctly by running the test suite.

The configuration file is located in the root directory. You need to edit it to set your API key and other preferences. The default settings should work for most common use cases.
